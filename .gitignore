# 비밀 정보 (절대 GitHub에 올리지 말 것)
.env
.env.local
.env.*.local

# 설치된 라이브러리들 (용량 크고 다시 받을 수 있음)
node_modules

# 빌드 결과물 (자동 생성됨)
dist
build

# macOS가 자동으로 만드는 파일
.DS_Store

# 개인 에디터 설정
.vscode
.idea
.claude

# 로그 파일
*.log
npm-debug.log*
.vercel

# Capacitor/iOS
ios/App/Pods
ios/App/build
ios/DerivedData
ios/App/App/public
ios/App/Podfile.lock
assets/
scripts/admin.jpeg
